info:
  name: Get Profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/users"
  headers:
    - name: Accept-Language
      value: en
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
