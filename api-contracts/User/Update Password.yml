info:
  name: Update Password
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{apiUrl}}/users"
  body:
    type: json
    data: |-
      {
        "currentPassword": "123456781",
        "newPassword": "1234567811"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
