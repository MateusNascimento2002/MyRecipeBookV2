info:
  name: Get Recents
  type: http
  seq: 5

http:
  method: GET
  url: "{{apiUrl}}/recipes/recent"
  headers:
    - name: accept
      value: text/plain
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
